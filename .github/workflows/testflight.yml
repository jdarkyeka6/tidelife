name: TideLife to TestFlight

on:
  workflow_dispatch: {}
  push:
    branches: [main]
    paths:
      - .github/workflows/testflight.yml
      - project.yml
      - TideLife/**

permissions:
  contents: read

concurrency:
  group: tidelife-testflight
  cancel-in-progress: false

jobs:
  build-and-upload:
    name: Build and upload iOS
    runs-on: macos-15
    timeout-minutes: 60
    env:
      BUNDLE_ID: com.jake.tidelife
      APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
      ASC_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
      ASC_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
      DEVELOPER_DIR: /Applications/Xcode_26.3.app/Contents/Developer

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Normalize and check Apple secrets
        shell: bash
        env:
          ASC_API_KEY_P8: ${{ secrets.APP_STORE_CONNECT_API_KEY }}
        run: |
          set -euo pipefail

          CLEAN_TEAM_ID=$(printf '%s' "$APPLE_TEAM_ID" | tr -d '[:space:]')
          CLEAN_KEY_ID=$(printf '%s' "$ASC_KEY_ID" | tr -d '[:space:]')
          CLEAN_ISSUER_ID=$(printf '%s' "$ASC_ISSUER_ID" | tr -d '[:space:]')

          missing=0
          [ -n "$CLEAN_TEAM_ID" ] || { echo "::error::Missing APPLE_TEAM_ID"; missing=1; }
          [ -n "$CLEAN_KEY_ID" ] || { echo "::error::Missing APP_STORE_CONNECT_API_KEY_ID"; missing=1; }
          [ -n "$CLEAN_ISSUER_ID" ] || { echo "::error::Missing APP_STORE_CONNECT_ISSUER_ID"; missing=1; }
          [ -n "$ASC_API_KEY_P8" ] || { echo "::error::Missing APP_STORE_CONNECT_API_KEY"; missing=1; }
          [ "$missing" -eq 0 ] || exit 1

          echo "APPLE_TEAM_ID=$CLEAN_TEAM_ID" >> "$GITHUB_ENV"
          echo "ASC_KEY_ID=$CLEAN_KEY_ID" >> "$GITHUB_ENV"
          echo "ASC_ISSUER_ID=$CLEAN_ISSUER_ID" >> "$GITHUB_ENV"

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Prepare App Store Connect API key
        shell: bash
        env:
          ASC_API_KEY_P8: ${{ secrets.APP_STORE_CONNECT_API_KEY }}
        run: |
          set -euo pipefail
          KEY_PATH="$RUNNER_TEMP/AuthKey_${ASC_KEY_ID}.p8"

          if printf '%s' "$ASC_API_KEY_P8" | grep -q 'BEGIN PRIVATE KEY'; then
            printf '%s' "$ASC_API_KEY_P8" > "$KEY_PATH"
          else
            ASC_API_KEY_P8="$ASC_API_KEY_P8" KEY_PATH="$KEY_PATH" python3 - <<'PY'
          import base64, os, pathlib, sys
          raw = ''.join(os.environ['ASC_API_KEY_P8'].split())
          try:
              decoded = base64.b64decode(raw, validate=True)
          except Exception as exc:
              sys.exit(f'APP_STORE_CONNECT_API_KEY is neither a raw .p8 key nor valid base64: {exc}')
          if b'BEGIN PRIVATE KEY' not in decoded:
              sys.exit('APP_STORE_CONNECT_API_KEY did not decode to a valid .p8 private key')
          pathlib.Path(os.environ['KEY_PATH']).write_bytes(decoded)
          PY
          fi

          chmod 600 "$KEY_PATH"
          echo "ASC_KEY_PATH=$KEY_PATH" >> "$GITHUB_ENV"

          mkdir -p "$HOME/.appstoreconnect/private_keys"
          cp "$KEY_PATH" "$HOME/.appstoreconnect/private_keys/AuthKey_${ASC_KEY_ID}.p8"

      - name: Verify Xcode 26 SDK and project
        shell: bash
        run: |
          set -euo pipefail
          xcodebuild -version
          SDK_VERSION="$(xcrun --sdk iphoneos --show-sdk-version)"
          echo "iOS SDK: $SDK_VERSION"
          case "$SDK_VERSION" in
            26.*) ;;
            *) echo "::error::Expected iOS 26 SDK, got $SDK_VERSION"; exit 1 ;;
          esac
          xcodebuild -project TideLife.xcodeproj -scheme TideLife -showBuildSettings | head -n 80

      - name: Archive TideLife with automatic provisioning
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p build
          xcodebuild \
            -project TideLife.xcodeproj \
            -scheme TideLife \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath "$PWD/build/TideLife.xcarchive" \
            DEVELOPMENT_TEAM="$APPLE_TEAM_ID" \
            PRODUCT_BUNDLE_IDENTIFIER="$BUNDLE_ID" \
            CURRENT_PROJECT_VERSION="$GITHUB_RUN_NUMBER" \
            CODE_SIGN_STYLE=Automatic \
            -allowProvisioningUpdates \
            -authenticationKeyPath "$ASC_KEY_PATH" \
            -authenticationKeyID "$ASC_KEY_ID" \
            -authenticationKeyIssuerID "$ASC_ISSUER_ID" \
            archive 2>&1 | tee "$PWD/build/xcode-archive.log"

      - name: Create export options
        shell: bash
        run: |
          cat > "$RUNNER_TEMP/ExportOptions.plist" <<PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>app-store-connect</string>
            <key>signingStyle</key>
            <string>automatic</string>
            <key>teamID</key>
            <string>${APPLE_TEAM_ID}</string>
            <key>uploadSymbols</key>
            <true/>
          </dict>
          </plist>
          PLIST

      - name: Export and sign IPA for App Store Connect
        shell: bash
        run: |
          set -euo pipefail
          xcodebuild \
            -exportArchive \
            -archivePath "$PWD/build/TideLife.xcarchive" \
            -exportPath "$PWD/build/export" \
            -exportOptionsPlist "$RUNNER_TEMP/ExportOptions.plist" \
            -allowProvisioningUpdates \
            -authenticationKeyPath "$ASC_KEY_PATH" \
            -authenticationKeyID "$ASC_KEY_ID" \
            -authenticationKeyIssuerID "$ASC_ISSUER_ID"

          IPA_PATH=$(find "$PWD/build/export" -maxdepth 1 -name '*.ipa' -print -quit)
          [ -n "$IPA_PATH" ] || { echo "::error::No IPA was produced"; exit 1; }
          echo "IPA_PATH=$IPA_PATH" >> "$GITHUB_ENV"

      - name: Upload to App Store Connect / TestFlight
        shell: bash
        run: |
          set -euo pipefail
          xcrun altool \
            --upload-app \
            --type ios \
            --file "$IPA_PATH" \
            --apiKey "$ASC_KEY_ID" \
            --apiIssuer "$ASC_ISSUER_ID"

      - name: Upload build diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tidelife-build-${{ github.run_number }}
          path: |
            build/xcode-archive.log
            build/export/*.ipa
          if-no-files-found: warn

      - name: Done
        if: success()
        run: echo "TideLife was uploaded to App Store Connect. It will appear in TestFlight after Apple finishes processing it."
